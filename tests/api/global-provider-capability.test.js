const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm"),root=path.join(__dirname,"../../apps/desktop/src/renderer/core"),w={};w.window=w;const c=vm.createContext({window:w,Object,Array,String,Boolean,RegExp,Math,Set,Number});["globalCommerceInputGuard.js","globalProviderCapability.js"].forEach(f=>vm.runInContext(fs.readFileSync(path.join(root,f),"utf8"),c));const api=w.WeishanGlobalProviderCapability;assert.equal(api.validateProviderCapabilities(["SEARCH","PRICE_INFORMATION"]).success,true);assert.equal(api.validateProviderCapabilities(["PAYMENT"]).success,false);assert.equal(api.validateProviderCapabilities(["CHECKOUT"]).success,false);assert.equal(api.validateProviderCapabilities(["ORDER"]).success,false);assert.equal(api.validateProviderCapabilities(["FULFILLMENT"]).success,false);console.log("GLOBAL_PROVIDER_CAPABILITY PASS");
