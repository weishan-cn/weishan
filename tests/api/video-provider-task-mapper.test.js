const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("path"),vm=require("node:vm");const w={};w.window=w;vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/videoProviderTaskMapper.js"),"utf8"),vm.createContext({window:w}));const t=w.WeishanVideoProviderTaskMapper.mapVideoProviderTask({taskId:"a",status:"WHATEVER",providerStatus:"hidden"});assert.equal(t.status,"FAILED");assert.equal(t.providerStatus,undefined);console.log("VIDEO_PROVIDER_TASK_MAPPER PASS");
