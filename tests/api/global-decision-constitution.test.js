const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm"),root=path.join(__dirname,"../../apps/desktop/src/renderer/core"),w={};w.window=w;const c=vm.createContext({window:w,Object,Array,String,Boolean,RegExp,Math,Set,Number});vm.runInContext(fs.readFileSync(path.join(root,"globalDecisionConstitution.js"),"utf8"),c);const api=w.WeishanGlobalDecisionConstitution,constitution=api.getDecisionConstitution();assert.equal(constitution.version,"1.0.0");assert.equal(constitution.articles.length,12);assert.equal(new Set(constitution.articles.map(a=>a.id)).size,12);assert.equal(Object.isFrozen(constitution),true);assert.equal(Object.isFrozen(constitution.articles),true);assert.equal(constitution.articles.every(a=>Object.isFrozen(a)&&typeof a.id==="string"&&typeof a.title==="string"&&typeof a.description==="string"&&typeof a.rationale==="string"),true);assert.doesNotThrow(()=>JSON.stringify(constitution));const text=JSON.stringify(constitution);for(let i=0;i<20;i+=1)assert.equal(JSON.stringify(api.getDecisionConstitution()),text);console.log("GLOBAL_DECISION_CONSTITUTION PASS");
