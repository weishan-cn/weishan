const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm");const w={};w.window=w;vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/videoWorkspaceEvents.js"),"utf8"),vm.createContext({window:w}));const E=w.WeishanVideoWorkspaceEvents,event=E.createVideoWorkspaceEvent("TaskCreated",{taskId:"a",providerId:"hidden",nested:{x:1}});assert.equal(Object.isFrozen(event),true);assert.equal(event.payload.taskId,"a");assert.equal(event.payload.providerId,undefined);const bus=E.createVideoWorkspaceEventBus();let seen=0;bus.subscribe(()=>seen++);bus.emit("DraftChanged",{valid:true});assert.equal(seen,1);assert.equal(bus.getCount(),1);console.log("VIDEO_WORKSPACE_EVENTS PASS");
