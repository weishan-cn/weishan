const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("path"),vm=require("node:vm");const w={};w.window=w;vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/videoProviderLifecycle.js"),"utf8"),vm.createContext({window:w}));const l=w.WeishanVideoProviderLifecycle.createVideoProviderLifecycle();assert.equal(l.getState(),"initialize");assert.equal(l.busy(),false);assert.equal(l.ready(),true);assert.equal(l.getState(),"ready");assert.equal(l.busy(),true);assert.equal(l.dispose(),true);assert.equal(l.getState(),"dispose");assert.equal(l.ready(),false);assert.equal(l.dispose(),true);console.log("VIDEO_PROVIDER_LIFECYCLE PASS");
