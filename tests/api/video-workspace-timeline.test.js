const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("path"),vm=require("node:vm");const w={};w.window=w;vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/videoWorkspaceTimeline.js"),"utf8"),vm.createContext({window:w}));const list=w.WeishanVideoWorkspaceTimeline.createVideoWorkspaceTimeline({status:"COMPLETED",providerId:"hidden"});assert.equal(list.find(item=>item.state==="current").label,"已完成");assert.equal(JSON.stringify(list).includes("provider"),false);console.log("VIDEO_WORKSPACE_TIMELINE PASS");
