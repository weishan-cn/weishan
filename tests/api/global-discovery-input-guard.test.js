const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm");const w={};w.window=w;const c=vm.createContext({window:w});vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/globalDiscoveryInputGuard.js"),"utf8"),c);const g=w.WeishanGlobalDiscoveryInputGuard;assert.equal(g.validateGlobalDiscoveryInput({paymentRegion:"CN",paymentTiming:"prepaid"}).valid,true);const getter={};Object.defineProperty(getter,"query",{get(){throw new Error("must not run")}});assert.equal(g.validateGlobalDiscoveryInput(getter).code,"UNSAFE_GETTER");const setter={};Object.defineProperty(setter,"query",{set(){}});assert.equal(g.validateGlobalDiscoveryInput(setter).code,"UNSAFE_SETTER");const circular={};circular.self=circular;assert.equal(g.validateGlobalDiscoveryInput(circular).code,"CIRCULAR_REFERENCE");assert.equal(g.validateGlobalDiscoveryInput({token:"x"}).code,"SENSITIVE_FIELD");assert.equal(g.validateGlobalDiscoveryInput({value:Infinity}).code,"INVALID_NUMBER");console.log("GLOBAL_DISCOVERY_INPUT_GUARD PASS");
