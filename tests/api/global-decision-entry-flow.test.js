const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm"),root=path.join(__dirname,"../../apps/desktop/src/renderer/core"),w={};w.window=w;const c=vm.createContext({window:w,Object,Array,String,Boolean,RegExp,Math,Set,Number});["globalCommerceInputGuard.js","globalDecisionIntent.js","globalDecisionConversationEntry.js","globalDecisionStarterExperience.js"].forEach(f=>vm.runInContext(fs.readFileSync(path.join(root,f),"utf8"),c));const e=w.WeishanGlobalDecisionConversationEntry.createConversationEntry({question:"I want to travel to Japan"});assert.equal(e.entry.domainCandidates.includes("TRAVEL"),true);assert.equal(e.entry.userDecisionRequired,true);assert.equal(w.WeishanGlobalDecisionStarterExperience.listDecisionStarters().purchaseInducement,false);console.log("GLOBAL_DECISION_ENTRY_FLOW PASS");
