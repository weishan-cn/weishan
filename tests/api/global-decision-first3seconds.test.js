const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm"),root=path.join(__dirname,"../../apps/desktop/src/renderer/core"),w={};w.window=w;const c=vm.createContext({window:w,Object,Array,String,Boolean,RegExp,Math,Set,Number});vm.runInContext(fs.readFileSync(path.join(root,"globalDecisionHabit.js"),"utf8"),c);const g=w.WeishanGlobalDecisionHabit.createDecisionHabitGuide().guide;assert.equal(g.coreQuestion,"What problem would you like to solve?");assert.equal(g.workspaceRequired,true);assert.equal(g.complexMenuShown,false);console.log("GLOBAL_DECISION_FIRST3SECONDS PASS");
