const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("path"),vm=require("node:vm");const root=path.resolve(__dirname,"../.."),w={};w.window=w;const c=vm.createContext({window:w});["videoProviderCapabilities.js","videoProviderTaskMapper.js","videoProviderArtifacts.js","videoProviderErrors.js","videoProviderValidator.js"].forEach(f=>vm.runInContext(fs.readFileSync(path.join(root,"apps/desktop/src/renderer/core",f),"utf8"),c));const V=w.WeishanVideoProviderValidator;assert.equal(V.validateVideoProviderEnvelope({ok:true,data:{taskId:"a",status:"RUNNING"}},"task").data.status,"GENERATING");assert.equal(V.validateVideoProviderEnvelope({ok:true,data:{taskId:"a",raw:"x"}},"task").ok,false);assert.equal(V.validateVideoProviderEnvelope({oops:true},"task").ok,false);let read=false;const getter={ok:true,data:{taskId:"a",status:"QUEUED"}};Object.defineProperty(getter.data,"title",{enumerable:true,get(){read=true;return"x";}});assert.equal(V.validateVideoProviderEnvelope(getter,"task").ok,false);assert.equal(read,false);const cyclic={ok:true,data:{taskId:"a",status:"QUEUED"}};cyclic.data.self=cyclic;assert.equal(V.validateVideoProviderEnvelope(cyclic,"task").ok,false);console.log("VIDEO_PROVIDER_VALIDATOR PASS");
