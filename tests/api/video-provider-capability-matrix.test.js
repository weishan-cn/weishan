const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm");const w={};w.window=w;vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/videoProviderCapabilityMatrix.js"),"utf8"),vm.createContext({window:w}));const M=w.WeishanVideoProviderCapabilityMatrix,workspace={capabilities:{textToVideo:true,imageToVideo:false},limits:{duration:15,resolution:["720p","1080p"]}},narrow=M.createVideoProviderCapabilityMatrix(workspace,{capabilities:{textToVideo:true,imageToVideo:true},limits:{duration:8,resolution:["720p"]}}),wide=M.createVideoProviderCapabilityMatrix(workspace,{capabilities:{textToVideo:true},limits:{duration:30,resolution:["4k","720p"]}});assert.equal(narrow.matrix.textToVideo.compatible,true);assert.equal(narrow.matrix.imageToVideo.compatible,false);assert.equal(narrow.matrix.duration.effectiveLimits,8);assert.deepEqual(Array.from(narrow.matrix.resolution.effectiveLimits),["720p"]);assert.equal(wide.matrix.duration.effectiveLimits,15);assert.deepEqual(Array.from(wide.matrix.resolution.effectiveLimits),["720p"]);assert.equal(Object.isFrozen(narrow.matrix),true);console.log("VIDEO_PROVIDER_CAPABILITY_MATRIX PASS");
