const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm");const w={};w.window=w;const c=vm.createContext({window:w});for(const f of ["globalDiscoveryInputGuard.js","globalDiscoveryEngine.js"])vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core",f),"utf8"),c);const api=w.WeishanGlobalDiscoveryEngine;const functions=["resolveDiscoveryRegion","createProviderCapabilityContract","validateProviderCapabilityContract","selectDiscoveryProviders","normalizeDiscoveryCandidates","buildDiscoveryComparison","createRedirectIntent","createGlobalDiscoveryPlan"];for(const name of functions){const bad={};Object.defineProperty(bad,"query",{get(){throw Error("getter")}});const result=api[name](bad);assert.equal(result.error.code,"NORMALIZATION_REJECTED",name);}assert.equal(api.normalizeDiscoveryCandidate("product",{token:"x"}).error.detailsSummary,"SENSITIVE_FIELD");const circular={};circular.self=circular;assert.equal(api.buildDiscoveryComparison(circular).error.detailsSummary,"CIRCULAR_REFERENCE");const safe=api.resolveDiscoveryRegion({domain:"flight",departure:"CN",arrival:"JP",paymentRegion:"CN",paymentTiming:"prepaid",paymentMethodLabel:"demo",endpointDisplayLabel:"display only"});assert.equal(safe.primaryMarket,"JP");const input={domain:"product",shippingDestination:"JP"},result=api.resolveDiscoveryRegion(input);input.shippingDestination="US";assert.equal(result.primaryMarket,"JP");console.log("GLOBAL_DISCOVERY_PUBLIC_API_GUARD PASS");
