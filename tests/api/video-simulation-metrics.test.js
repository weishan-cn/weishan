const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("path"),vm=require("node:vm");const w={};w.window=w;vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/videoSimulationMetrics.js"),"utf8"),vm.createContext({window:w}));const m=w.WeishanVideoSimulationMetrics.createVideoSimulationMetrics([{status:"COMPLETED",steps:2},{status:"FAILED",steps:4}]);assert.equal(m.successRate,.5);assert.equal(m.averageSteps,3);console.log("VIDEO_SIMULATION_METRICS PASS");
