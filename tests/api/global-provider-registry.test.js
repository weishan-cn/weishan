const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm"),root=path.join(__dirname,"../../apps/desktop/src/renderer/core"),w={};w.window=w;const c=vm.createContext({window:w,Object,Array,String,Boolean,RegExp,Math,Set,Number});["globalProviderRegistry.js"].forEach(f=>vm.runInContext(fs.readFileSync(path.join(root,f),"utf8"),c));const providers=w.WeishanGlobalProviderRegistry.listGlobalProviders();assert.equal(providers.length>=4,true);assert.equal(providers.every(p=>p.providerType==="REFERENCE_ONLY"&&p.runtimeConnected===false&&p.rankingControlled===false),true);assert.equal(JSON.stringify(providers).includes("token"),false);console.log("GLOBAL_PROVIDER_REGISTRY PASS");
