const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("path"),vm=require("node:vm");const w={};w.window=w;vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/videoSimulationScheduler.js"),"utf8"),vm.createContext({window:w}));let calls=0;const s=w.WeishanVideoSimulationScheduler.createVideoSimulationScheduler({advance:()=>++calls});s.tick("a");s.step("a");assert.equal(calls,2);console.log("VIDEO_SIMULATION_SCHEDULER PASS");
