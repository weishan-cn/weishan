const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm");const w={};w.window=w;vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/videoProviderComplianceMatrix.js"),"utf8"),vm.createContext({window:w}));const M=w.WeishanVideoProviderComplianceMatrix,m=M.createVideoProviderComplianceMatrix({contract:{status:"passed",code:"OK",message:"接口通过"},security:{status:"bad"}});assert.deepEqual(Object.keys(m),Array.from(M.KEYS));assert.equal(m.contract.status,"passed");assert.equal(m.security.status,"not_run");console.log("VIDEO_PROVIDER_COMPLIANCE_MATRIX PASS");
