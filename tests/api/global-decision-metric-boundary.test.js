const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm"),w={};w.window=w;const c=vm.createContext({window:w,Object,Array,String,Boolean,RegExp,Math,Set,Number});vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/globalDecisionMetricBoundary.js"),"utf8"),c);const evaluate=w.WeishanGlobalDecisionMetricBoundary.evaluateMetricBoundary;assert.equal(evaluate({metrics:["TIME_SAVED"]}).status,"METRIC_BOUNDARY_PASS");assert.equal(evaluate({metrics:["TIME_SPENT"]}).status,"METRIC_BOUNDARY_REJECTED");assert.equal(evaluate({metrics:["UNKNOWN"]}).status,"METRIC_BOUNDARY_REJECTED");console.log("GLOBAL_DECISION_METRIC_BOUNDARY PASS");
