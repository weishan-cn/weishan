const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm");const root=path.resolve(__dirname,"../..");const window={};window.window=window;vm.runInContext(fs.readFileSync(path.join(root,"apps/desktop/src/renderer/core/videoWorkspaceValidation.js"),"utf8"),vm.createContext({window}));const V=window.WeishanVideoWorkspaceValidation,d=V.createVideoDraft();assert.equal(d.duration,5);const good=V.validateVideoDraft({...d,title:"T",prompt:"P"});assert.equal(good.valid,true);assert.equal(V.validateVideoDraft({...d,title:"",prompt:"P"}).valid,false);assert.equal(V.validateVideoDraft({...d,title:"T",prompt:"x".repeat(4001)}).valid,false);assert.equal(V.validateVideoDraft({...d,title:"T",prompt:"P",duration:31}).valid,false);assert.equal(V.validateVideoDraft({...d,title:"T",prompt:"P",resolution:"bad"}).valid,false);assert.equal(V.mergeLimits({prompt:2000}).prompt,2000);assert.equal(V.mergeLimits({prompt:9000}).prompt,4000);console.log("VIDEO_WORKSPACE_VALIDATION PASS");
