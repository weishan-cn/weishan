const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("path"),vm=require("node:vm");const w={};w.window=w;vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/videoSimulationSnapshots.js"),"utf8"),vm.createContext({window:w}));const s=w.WeishanVideoSimulationSnapshots.createVideoSimulationSnapshots();s.record({tasks:[{id:"a"}]});assert.equal(s.list().length,1);s.reset();assert.equal(s.list().length,0);console.log("VIDEO_SIMULATION_SNAPSHOTS PASS");
