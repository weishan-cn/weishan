const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("path"),vm=require("node:vm");const root=path.resolve(__dirname,"../.."),w={};w.window=w;const c=vm.createContext({window:w});["videoSimulationScenario.js","videoSimulationAssets.js","videoSimulationProgress.js","videoSimulationMetrics.js","videoSimulationSnapshots.js","videoSimulationEngine.js"].forEach(f=>vm.runInContext(fs.readFileSync(path.join(root,"apps/desktop/src/renderer/core",f),"utf8"),c));const e=w.WeishanVideoSimulationEngine.createVideoSimulationEngine(),t=e.createTask({title:"T"});assert.equal(t.taskId,"simulation-task-000001");e.advance(t.taskId);e.complete(t.taskId);assert.equal(e.getState().tasks[0].status,"COMPLETED");assert.equal(e.getState().tasks[0].artifacts.length,3);assert.equal(e.getSnapshots().length>0,true);console.log("VIDEO_SIMULATION_ENGINE PASS");
