const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("path"),vm=require("node:vm");const w={};w.window=w;vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/videoProviderCapabilities.js"),"utf8"),vm.createContext({window:w}));const c=w.WeishanVideoProviderCapabilities.createVideoProviderCapabilities({textToVideo:true,endpoint:true});assert.equal(c.textToVideo,true);assert.equal(c.endpoint,undefined);console.log("VIDEO_PROVIDER_CAPABILITIES PASS");
