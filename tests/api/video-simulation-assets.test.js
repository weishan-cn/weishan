const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("path"),vm=require("node:vm");const w={};w.window=w;vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/videoSimulationAssets.js"),"utf8"),vm.createContext({window:w}));const a=w.WeishanVideoSimulationAssets.createVideoSimulationAssets("x");assert.equal(a.length,3);assert.equal(a[0].availability,false);assert.equal(a[0].path,undefined);console.log("VIDEO_SIMULATION_ASSETS PASS");
