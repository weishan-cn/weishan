const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm"),r=path.join(__dirname,"../../apps/desktop/src/renderer/core"),w={};w.window=w;const c=vm.createContext({window:w,Object,Array,String,Boolean,RegExp,Math,Set,Number});["globalCommerceInputGuard.js","globalDecisionDesignSystem.js"].forEach(f=>vm.runInContext(fs.readFileSync(path.join(r,f),"utf8"),c));const audit=w.WeishanGlobalDecisionDesignSystem.auditDesignSequence;const getter=[];Object.defineProperty(getter,"0",{get(){return "QUESTION";}});assert.equal(audit(getter).status,"DESIGN_SYSTEM_REJECTED");const circular=[];circular.push(circular);assert.equal(audit(circular).status,"DESIGN_SYSTEM_REJECTED");const good=["QUESTION","EVIDENCE","RISKS","RECOMMENDATION","USER_DECISION","SAVE_OR_EXIT"],result=JSON.stringify(audit(good));for(let i=0;i<20;i++)assert.equal(JSON.stringify(audit(good)),result);assert.equal(Object.isFrozen(audit(good)),true);console.log("GLOBAL_DECISION_PHASE_C_SECURITY PASS");
