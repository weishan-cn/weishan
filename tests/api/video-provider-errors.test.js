const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("path"),vm=require("node:vm");const w={};w.window=w;vm.runInContext(fs.readFileSync(path.join(__dirname,"../../apps/desktop/src/renderer/core/videoProviderErrors.js"),"utf8"),vm.createContext({window:w}));const e=w.WeishanVideoProviderErrors.createVideoProviderError({code:"RATE_LIMITED",message:"raw"});assert.equal(e.message,"请求过于频繁，请稍后重试");assert.equal(e.raw,undefined);console.log("VIDEO_PROVIDER_ERRORS PASS");
