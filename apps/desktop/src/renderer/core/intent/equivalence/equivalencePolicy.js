(function(){const V=window.WeishanIntentValidation;const POLICY=V.freeze({version:"1.0",minimumCorpus:240,minimumPrimaryCompatibility:0.98,minimumDestinationCompatibility:0.97});function evaluateReadiness(metrics,frameworkTestsPass){let m=V.json(metrics,true),gates={A:m.totalCases>=POLICY.minimumCorpus,B:frameworkTestsPass===true,C:m.safetyCriticalMismatchCount===0,D:(m.perDifferenceCode.SHADOW_CONFIRMATION_WEAKER||0)===0,E:(m.perDifferenceCode.SHADOW_CAPABILITY_MORE_PERMISSIVE||0)===0,F:m.primaryIntentCompatibleRate>=POLICY.minimumPrimaryCompatibility,G:m.destinationMatchRate>=POLICY.minimumDestinationCompatibility,H:m.comparableCases>0&&m.confirmationExactMatchRate===1,I:(m.perDifferenceCode.SHADOW_UNDER_ASKS||0)===0,J:m.comparableCases>0};let decision=!gates.A||!gates.B?"NOT_READY":m.comparableCases===0?"INSUFFICIENT_BASELINE":Object.values(gates).every(Boolean)?"READY_FOR_PHASE_3_DESIGN":"NOT_READY";return V.freeze({policyVersion:POLICY.version,gates:V.freeze(gates),decision})}window.WeishanEquivalencePolicy=Object.freeze({POLICY,evaluateReadiness});})();
